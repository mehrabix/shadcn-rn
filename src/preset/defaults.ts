export interface PresetDefaults {
  style: string
  baseColor: string
  cssVariables: boolean
}

export const presetDefaults: PresetDefaults = {
  style: "default",
  baseColor: "neutral",
  cssVariables: true,
}
